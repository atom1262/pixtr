template = "<%=j button_to "Like", like_group_path(@group), remote: true %>";
$("#like-button").html(template);

counter = "<%=j pluralize(@group.likes.count, "like") %>";
$("#like-count").html(counter);